{
  "name": "electrica",
  "private": true,
  "scripts": {
    "dev": "npx nodemon backend/index.js",
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "vercel-build": "npm ci --include=dev && cd frontend && npm ci --include=dev && npm run build"
  },
  "dependencies": {
    "nodemon": "^3.1.9"
  }
}
