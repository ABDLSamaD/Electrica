{
  "name": "electrica",
  "private": true,
  "scripts": {
    "install": "npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "cd backend && npm start",
    "vercel-build": "cd backend && npm install --production && npm run build && cd ../frontend && npm install --production && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
