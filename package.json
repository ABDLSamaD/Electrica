{
  "name": "electrica",
  "private": true,
  "scripts": {
    "dev": "npx nodemon backend/index.js",
    "start": "cd backend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "npm install --production && npm run build"
  },
  "dependencies": {
    "nodemon": "^3.1.9"
  }
}
